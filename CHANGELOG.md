# CHANGELOG

## Unreleased

* Диалоги вынесены в **JSON** (`dialogue.json`).
* Подключение диалогов через **trait** (`DialogBehavior`).
* Добавлена поддержка кастомного поведения NPC (Citizens 2).
* Централизованное управление через `DialogManager`.
* Игрок при диалоге **замораживается**; управление опциями: **W / S / SPACE**.
* Поддержка сохранения прогресса и выхода из диалога.
* Поддержка нескольких сессий (ключ: `npcId + playerUUID`).

## v1.4.2 — прототип

* Частичный хардкод диалога нпс.
* Простейший `DialogSession` на `Map<Player,...>`.
* Отображение опций в ActionBar.
* Управление через SteerVehicle без сохранения прогресса.

## TODO

* Вынести подключения диалога к нпс из кода.
* Поправить баги в логике диалогов.
* Реализовать `conditions` и `effects` у опций.
* Команда `/dialog reload` для горячей перезагрузки.